# Atualização rápida V4 → V5

O link do Render e o PostgreSQL continuam os mesmos.

No projeto V5, mantenha o seu arquivo `.env` local. Depois execute no CMD do VS Code:

```bat
git add .
git commit -m "Central de Chamados V5"
git push origin main
```

O Render deverá iniciar um deploy automático. Não crie um banco novo.
